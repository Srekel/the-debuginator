pub usingnamespace @cImport({
    @cInclude("the_debuginator_wrapper.h");
});

// pub usingnamespace @cImport("c.c");

// const local = struct {
// export fn debuginator_assert(condition: bool) void {
//     if (!condition) unreachable;
// }
// export fn de buginator_sprintf_s(varargs: anytype) void {}
// export fn debuginator_strcpy_s(varargs: anytype) void {}
// };
